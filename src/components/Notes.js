import React from "react";
import Notepad from "./Notepad";

const Notes = () => {
  return (
    <Notepad content="I can't believe I am getting so good so quickly. I will beat Magnus in no time!"/>
  );
};

export default Notes;
